# 最初に行うこと

## インストール

展開したフォルダーをターミナルで開き、実行します。依存パッケージのダウンロードと、`~/.agents/skills` へのスキル追加を行います。既存スキルは上書きしません。

```sh
python3 install.py
```

Codexを再起動し、次のように依頼してください。

> このフォルダーのSTART-HERE.mdを読み、$instagram-insights-onboarding を使ってセットアップしてください。

## 本人が準備する設定

- 本人が管理するInstagramプロアカウントと接続するFacebookページ。
- Metaアプリ・必要な権限・本人資産を読む有効なトークン。画面や条件は公式資料でその都度確認。
- Google CloudプロジェクトのSheets API・Drive API、有効なOAuth同意設定とデスクトップアプリ用認証JSON。
- 認証JSONをこのフォルダーの `credentials.json` に保存。
- `.env` にアカウントIDとトークンを本人がローカルで設定。値をチャットに貼らない。

`GOOGLE_SPREADSHEET_ID` は空欄にします。認証後、専用の新規シートを作り、IDを `data/<アカウントID>/ownership.json` に保存して次回も再利用します。シートURLはこのIDから `https://docs.google.com/spreadsheets/d/<ID>/edit` として確認できます。この記録を手で編集したり、別アカウントへコピーしないでください。既存の業務シートを指定すると停止します。

旧版の履歴・認証を使っている場合は、旧ジョブを停止し、別フォルダーへ新しいキットを展開してください。旧データは自動移行しません。Google側で旧連携の権限を解除してから、新しいキットで `drive.file` だけを許可して再認証します。旧 token.json のコピーはしないでください。アカウントを切り替えるときも出力IDを空欄にし、専用の別シートを使います。

```sh
.venv/bin/python doctor.py
.venv/bin/python collector.py
```

初回はGoogleの同意画面が開きます。対象アカウントと権限を確認して本人が同意します。終了コードだけでなく、生成シート・対象名・件数・空欄・collection_statusの時刻と結果を確認してください。PARTIALの場合、取得できない指標を確認してから運用可否を判断します。

## 定期実行（任意）

手動同期が成功した後で予定を確認します。

```sh
.venv/bin/python install_automation.py
```

本人の希望を確認して登録します。

```sh
.venv/bin/python install_automation.py --apply
```

Macの現地時刻07:30、12:30、18:30、23:30に同じ収集処理を実行します。登録済みと稼働確認済みは別です。次の予定後に `logs/` とシートの更新時刻を確認してください。既存ジョブは上書きしません。

認証切れの場合は手動同期で再認証します。Metaのトークンは本人が更新します。Google OAuthの公開状態やテスト設定によって再認証が必要になる場合があります。

## トラブル・停止

エラーに秘密情報を含めないため、ログのAPI応答本文は省略しています。設定・権限・期限・ネットワークをローカルで確認します。Issueには秘密情報、実データ、未加工のAPI応答を添付しないでください。

停止はMacのLaunchAgentsにある `com.instagram-insights-kit.collect.plist` を対象に登録解除します。AIには対象ファイルの内容を確認させ、このキットのジョブだけを解除するよう依頼してください。フォルダーを移動すると定期実行が失敗するので、登録後は移動しないでください。
